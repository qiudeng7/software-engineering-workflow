# demand-change(tasks): 导出全部筛选结果

## 关联与当前状态

- 关联 Issue：#187
- Change 类型：`demand-change`
- 覆盖步骤：执行简报中的步骤 1–3
- 覆盖验收：筛选结果完整导出、权限一致、10,000 条上限、用户文档更新
- 当前状态：步骤 1–3 已完成，等待审查
- 关闭方式：本 PR 满足 #187 全部验收项，合并时使用 `Closes #187`

## 阶段报告

### 阶段 1：收敛共享查询规则

- 对应计划：#187 执行步骤 1
- 状态与结果：已完成；任务列表和导出共用筛选、权限与排序规则。
- 验证：`pnpm test -- task-query`，24 项通过。
- 计划偏差：无。
- 下一步：实现 CSV 导出接口。

### 阶段 2：实现 CSV 导出接口

- 对应计划：#187 执行步骤 2
- 状态与结果：已完成；同步导出保持 10,000 条上限。
- 验证：`pnpm test -- task-export-contract task-export-pagination`，20 项通过；Preview 导出 9,500 条匹配任务，无重复和遗漏。
- 计划偏差：执行简报原计划一次读取全部结果，实际改为按稳定游标分批读取并流式编码。原因是限制峰值内存；代价是实现复杂度增加。需求、外部接口、排序和数量上限均未改变，因此作为实现细节偏差留在 PR。
- 下一步：接入任务列表并完成端到端验收。

### 阶段 3：页面接入与验收

- 对应计划：#187 执行步骤 3
- 状态与结果：已完成；页面入口、错误提示和用户文档已接入。
- 验证：
  - `pnpm test:e2e -- task-export-limit`，3 项通过；
  - `pnpm lint && pnpm typecheck && pnpm build`，全部通过；
  - Preview 真实导出与页面筛选、排序一致。
- 计划偏差：接入时发现原设计复用通用 CSV 序列化器会包含内部字段，涉及数据暴露边界，因此暂停该步骤并同步到 #187 的“导出字段边界”讨论。确认改为字段白名单并更新系统设计后恢复执行。
- 下一步：进入审查。

## 验收结果

| Issue 验收项 | 验证方式与环境 | 结果 | 对应阶段报告 |
|---|---|---|---|
| 使用当前筛选、权限和排序 | 查询规则测试与 Preview 真实导出 | 通过 | 阶段 1、2 |
| 跨页结果不重不漏 | 分页测试及 9,500 条 Preview 数据核对 | 通过 | 阶段 2 |
| 超过 10,000 条时拒绝不完整导出 | 端到端测试 | 通过 | 阶段 3 |
| 用户文档与实际行为一致 | 文档链接检查和人工核对 | 通过 | 阶段 3 |
